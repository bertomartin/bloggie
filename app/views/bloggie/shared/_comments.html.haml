%hr
- if comments.any?
  %h3 #{comments.count} #{'Comment'.pluralize(comments.count)}
  - for comment in comments
    .comment
      #{comment.name} commented:
      = BlueCloth.new(comment.body).to_html.html_safe
